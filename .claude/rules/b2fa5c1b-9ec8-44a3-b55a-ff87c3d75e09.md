<rule_activation id="b2fa5c1b-9ec8-44a3-b55a-ff87c3d75e09" title="Standardize Public API Contract Design with Type-Safe Route Handlers and Internationalization: Route Handlers Use" applies_to="apps/remix/app/routes/**/*.tsx,packages/lib/server-only/**/*.ts,lingui.config.ts">
These rules are ALWAYS ACTIVE for all Remix route handlers, server-side PDF generation utilities, and internationalization configuration files.
</rule_activation>

### Rules

- **R-API-001** SHOULD: API route handlers SHOULD use TypeScript with explicit type definitions for loader and action functions

### Verify

```bash
# Count authenticated routes using _authenticated+ prefix
grep -r '_authenticated+' apps/remix/app/routes/ | wc -l

# Count route files implementing loader or action functions
find apps/remix/app/routes -name '*.tsx' -exec grep -l 'loader\|action' {} \; | wc -l

# Verify Lingui configuration presence and integration
grep -r 'lingui' . --include='*.config.*' --include='*.tsx' | head -5

# Locate PDF generation utilities in server-only packages
find packages/lib/server-only -name '*pdf*.ts' -type f
```

**Accept when:**
- All authenticated routes use `_authenticated+` prefix and implement proper authorization checks
- At least 90% of API routes follow the established URL parameter conventions (`$orgUrl`, `$teamUrl`)
- Lingui configuration is present and integrated into route handlers for internationalized responses
- PDF generation utilities are isolated in server-only packages with appropriate content-type handling

<enforcement>
Claude Code MUST NOT skip or defer verification. All route handlers MUST be scanned for TypeScript type definitions on loader and action functions. Violations block pull requests until TypeScript compilation succeeds without errors.
</enforcement>