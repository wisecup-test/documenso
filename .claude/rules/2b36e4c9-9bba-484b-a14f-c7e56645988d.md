<rule_activation id="2b36e4c9-9bba-484b-a14f-c7e56645988d" title="Adopt E2E API Testing for Frontend Rendering Model Validation: Tests Cover Versions" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching API endpoints, test specifications, and CI/CD configurations that serve frontend rendering components.
</rule_activation>

### Rules

- **R-E2E-001** MUST: Tests MUST cover all API versions (v1, v2, tRPC) that serve data to frontend components to ensure version consistency.

### Verify

```bash
# Verify E2E API test files exist
find packages/app-tests/e2e/api -name '*.spec.ts' | wc -l | grep -E '^[8-9]|^[1-9][0-9]+$'

# Count test cases across E2E API suite
grep -r 'describe\|test\|it' packages/app-tests/e2e/api --include='*.spec.ts' | wc -l

# List all E2E API test files
npm test -- --testPathPattern=e2e/api --listTests | wc -l
```

**Accept when:**
- At least 8 E2E API test specification files exist covering major API endpoints
- All API versions (v1, v2, tRPC) have corresponding test coverage for frontend-facing endpoints
- E2E test suite runs successfully in CI/CD pipeline with >90% pass rate
- Test coverage includes document operations, template management, envelope workflows, and search functionality

<enforcement>
Claude Code MUST NOT skip or defer verification. E2E API test coverage is mandatory for all API changes that serve frontend rendering components. Pull requests without corresponding E2E tests are blocked from merging. CI/CD pipeline must pass all E2E tests before deployment.
</enforcement>